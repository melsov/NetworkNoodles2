using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreBomb : MonoBehaviour {

    public Scoreboard.LedgerEntry le;

    private void Start() {
        //foreach (var scr in FindObjectsOfType<Score>()) {
        //    Debug.Log("score bomb for: " + scr.name);
        //    scr.receiveScoreBomb(le);
        //}
    }
    
}
